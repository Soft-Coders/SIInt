CREATE TABLE CLIENTE (ID BIGINT NOT NULL, DTYPE VARCHAR(31), CIUDAD VARCHAR NOT NULL, CODIGOPOSTAL INTEGER NOT NULL, DIRECCION VARCHAR NOT NULL, FECHA_ALTA DATE NOT NULL, FECHA_BAJA DATE, IDENTIFICACION VARCHAR NOT NULL, PAIS VARCHAR NOT NULL, ESTADO VARCHAR NOT NULL, TIPO_CLIENTE VARCHAR NOT NULL, PRIMARY KEY (ID))
CREATE TABLE EMPRESA (ID BIGINT NOT NULL, FECHA_NACIMIENTO VARCHAR(30) NOT NULL, PRIMARY KEY (ID))
CREATE TABLE INDIVIDUAL (ID BIGINT NOT NULL, APELLIDO VARCHAR(40) NOT NULL, FECHA_NACIMIENTO DATE, NOMBRE VARCHAR(30) NOT NULL, PRIMARY KEY (ID))
ALTER TABLE EMPRESA ADD CONSTRAINT FK_EMPRESA_ID FOREIGN KEY (ID) REFERENCES CLIENTE (ID)
ALTER TABLE INDIVIDUAL ADD CONSTRAINT FK_INDIVIDUAL_ID FOREIGN KEY (ID) REFERENCES CLIENTE (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT NUMERIC(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
